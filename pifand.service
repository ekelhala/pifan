[Unit]
Description=PWM fan control service
After=multi-user.target

[Service]
Type=simple
ExecStart=/usr/bin/python3 /usr/local/bin/pifand.py
Restart=on-failure
User=root

[Install]
WantedBy=multi-user.target